Demonstrates how to use a combination of controllers to select an object

1) touch_combo_free_choice allows users to map a touchscreen AND an eyetracker to then select a stim object with either controller.
2) The object selected, along with the controller number (1 or 2) will be displayed in the Matlab command window.